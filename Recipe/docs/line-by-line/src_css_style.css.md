File: src/css/style.css
Purpose: Minimal custom CSS overrides for the UI.

At a glance: Sets global typography, spacing, link styling.

Imports & dependencies: None.

Configuration/Constants:
- Applies `font-family` and `margin` to `<body>`.
- Adjusts layout for header nav, anchors, etc.

Main content:
- Straightforward CSS declarations; see HTML templates for usage (`link rel="stylesheet" href="/style.css"` not currently present, so confirm integration – flagged in `docs/COVERAGE_REPORT.md` as unused unless manually referenced).

Edge cases & errors:
- Not referenced in current templates; verify whether to link or delete.

Related files: `docs/line-by-line/src_server.js.md` (serves `/css`).
